{{ if eq .chezmoi.os "darwin" }}
vim.g.cpp_compiler = "clang++"
{{ end }}

{{ if eq .chezmoi.os "linux" }}
vim.g.cpp_compiler = "g++"
{{ end }}

-- Optional overrides for <leader>cr / :CppRun.
-- vim.g.cpp_build_flags = "-std=c++20 -Wall -Wextra -g"
-- vim.g.cpp_run_args = ""
-- vim.g.cpp_run_command = "cmake --build build && ./build/my_program"

-- Optional vim-dadbod connections. Keep credentials in this host-local file.
-- vim.g.dbs = {
--   local_postgres = os.getenv("DATABASE_URL"),
-- }
